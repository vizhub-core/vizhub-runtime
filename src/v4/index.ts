// TODO build this out to be similar to v2Build,
// but with the following changes:
// - Detect the <script type="module" > entry points in index.html
// - Use rollup to bundle the entry points
// - Use Sucrase to transform the entry points
// - Configure Rollup to output ESM
// - Generate an import map for packages
